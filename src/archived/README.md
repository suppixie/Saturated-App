# Archived features

`GroupChats.tsx` contains the complete Group Chats interface that was removed
from the active Saturated experience. It is intentionally retained here so the
feature can be restored later without rebuilding its screens and interactions.

The Feed entry point and active screen rendering were removed on 10 August
2026. Supabase group-chat tables and migrations are retained for compatibility.
